name: tic_tac_toe
description: A Flutter Tic Tac Toe Game with Scoreboard and Sounds
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  shared_preferences: ^2.1.1   # ✅ Added SharedPreferences

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/bgm.mp3
    - assets/Celebration.mp3
    - assets/Tap.mp3
