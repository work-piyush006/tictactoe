name: Flutter Android CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-android:
    runs-on: ubuntu-latest
    name: Build Android APK & AAB

    steps:
      # 1. Checkout repository
      - name: 1️⃣ Checkout repository
        uses: actions/checkout@v3

      # 2. Setup Flutter
      - name: 2️⃣ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.22.2"
          channel: "stable"
          cache: true

      # 3. Install dependencies
      - name: 3️⃣ Install dependencies
        run: flutter pub get

      # 4. Remove existing android folder and generate fresh Android project
      - name: 4️⃣ Generate fresh Android platform
        run: |
          echo "🛠 Removing existing android folder..."
          rm -rf android
          flutter create --platforms=android .

      # 5. Wait until AndroidManifest.xml exists
      - name: 5️⃣ Wait for AndroidManifest.xml
        run: |
          COUNT=0
          while [ ! -f "android/app/src/main/AndroidManifest.xml" ]; do
            sleep 1
            COUNT=$((COUNT+1))
            if [ $COUNT -gt 30 ]; then
              echo "❌ AndroidManifest.xml not found after 30s"
              exit 1
            fi
          done
          echo "✅ AndroidManifest.xml found"

      # 6. Add INTERNET permission
      - name: 6️⃣ Add Internet permission
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          if ! grep -q 'android.permission.INTERNET' $MANIFEST; then
            sed -i '/<application/i\    <uses-permission android:name="android.permission.INTERNET"/>' $MANIFEST
          fi

      # 7. Copy Firebase google-services.json if exists
      - name: 7️⃣ Copy Firebase config
        run: |
          if [ -f "firebase/google-services.json" ]; then
            cp firebase/google-services.json android/app/google-services.json
            echo "✅ google-services.json copied"
          else
            echo "⚠️ google-services.json not found, skipping..."
          fi

      # 8. Patch android/build.gradle (Google repos & services)
      - name: 8️⃣ Patch android/build.gradle
        run: |
          FILE="android/build.gradle"
          # Add google() repository
          if ! grep -q "google()" $FILE; then
            sed -i '/repositories {/a\        google()' $FILE
          fi
          # Add Google Services classpath
          if ! grep -q "com.google.gms:google-services" $FILE; then
            sed -i '/dependencies {/a\        classpath "com.google.gms:google-services:4.4.2"' $FILE
          fi
          # Set Kotlin version
          if ! grep -q "ext.kotlin_version" $FILE; then
            sed -i '/dependencies {/i\ext.kotlin_version = "1.9.10"' $FILE
          else
            sed -i 's/ext.kotlin_version = .*/ext.kotlin_version = "1.9.10"/' $FILE
          fi

      # 9. Patch android/app/build.gradle for Ads & Google Services safely
      - name: 9️⃣ Patch android/app/build.gradle
        run: |
          FILE="android/app/build.gradle"
          # Add play-services-ads if missing
          if ! grep -q "com.google.android.gms:play-services-ads" $FILE; then
            sed -i '/dependencies {/a\    implementation "com.google.android.gms:play-services-ads:23.4.0"' $FILE
          fi
          # Add apply plugin at bottom (after plugins block)
          if ! grep -q "com.google.gms.google-services" $FILE; then
            echo 'apply plugin: "com.google.gms.google-services"' >> $FILE
          fi
          # Ensure compileSdkVersion 35
          sed -i 's/compileSdkVersion .*/compileSdkVersion 35/' $FILE
          # Ensure targetSdkVersion 35
          sed -i 's/targetSdkVersion .*/targetSdkVersion 35/' $FILE

      # 10. Build APK
      - name: 1️⃣0️⃣ Build APK
        run: flutter build apk --release

      # 11. Build AAB
      - name: 1️⃣1️⃣ Build AAB
        run: flutter build appbundle --release

      # 12. Upload APK & AAB artifacts
      - name: 1️⃣2️⃣ Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            build/app/outputs/flutter-apk/*.apk
            build/app/outputs/bundle/release/*.aab
          retention-days: 7
